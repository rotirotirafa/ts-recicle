# Typescript

O (typescript)[https://www.typescriptlang.org/] é um superset do JS (javascript)

- Instalar:
```npm i -g typescript```

- Inicializar:
```tsc --init ```

- Watch file changes:
```tsc -w```
